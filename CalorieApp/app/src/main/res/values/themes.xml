<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CalorieApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.CalorieApp" parent="Base.Theme.CalorieApp" />
</resources>